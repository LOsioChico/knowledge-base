{
  "name": "audit-notes",
  "private": true,
  "type": "module",
  "description": "Spike: use Cursor SDK (composer-2) to audit knowledge-base notes against AGENTS.md.",
  "scripts": {
    "start": "tsx audit-notes.ts",
    "autofix": "tsx autofix.ts",
    "cache:clean": "tsx cache-clean.ts",
    "lint:content": "tsx pass0-all.ts",
    "lint:format": "prettier --check ../../content",
    "format": "prettier --write ../../content",
    "test": "node --import tsx --test extract-json.test.ts deterministic.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.11"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.3.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
