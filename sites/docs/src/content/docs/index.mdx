---
title: Knowledge Base
description: Effect-TS on Starlight + Twoslash; other areas still on Quartz during migration.
---

> Personal notes on TypeScript backends: NestJS, AWS, and Effect-TS. **Effect-TS** is the first area on Starlight (MDX, typed code blocks, sidebar nav); other areas still publish from the Obsidian vault via Quartz until migrated.

## Start here

- **Effect-TS** — [[effect-ts]] overview or [[effect-ts/quickstart]] (~10 min).
- **NestJS** — [Quartz area](https://losiochico.github.io/knowledge-base/nestjs/) (wikilinks, graph, audit).
- **AWS** — [Quartz area](https://losiochico.github.io/knowledge-base/aws/).

## Migration status

We are moving areas from Quartz (`content/**/*.md`) to Starlight MDX (`sites/docs/src/content/docs/`) **one note at a time**, re-authoring for Twoslash and Starlight components rather than copy-pasting.

| Area | Published here | Vault source |
| --- | --- | --- |
| Effect-TS | **Migrated** on Starlight (all area notes; vault stays source for audit) | `content/effect-ts/` |
| NestJS, AWS, … | Quartz only | `content/<area>/` |

Tracker: [`migration.json`](https://github.com/LOsioChico/knowledge-base/blob/main/sites/docs/migration.json) and [STARLIGHT-MIGRATION.md](https://github.com/LOsioChico/knowledge-base/blob/main/docs/STARLIGHT-MIGRATION.md).

## Local development

```bash
# Starlight (migrated pages)
cd sites/docs && bun install && bun run dev

# Vault lint + audit (unchanged)
bun run lint:wikilinks
bun run vault:check --base HEAD~1
```
