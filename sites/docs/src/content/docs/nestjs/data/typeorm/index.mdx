---
title: TypeORM
description: NestJS + TypeORM — TypeOrmModule, repositories, migrations, and Postgres error handling.
---

import { Aside, Card, CardGrid, Steps } from "@astrojs/starlight/components";

> NestJS + TypeORM patterns: integration glue (`TypeOrmModule`, `@InjectRepository`), error handling, and gotchas specific to running TypeORM under Nest's DI container.

## Read in this order

<Steps>

1. [[nestjs/data/typeorm/postgresql-setup|PostgreSQL setup with TypeORM]]: connection, entities, repositories, CRUD, migrations.
2. [[nestjs/data/typeorm/handle-database-errors|Handle database errors]]: map `QueryFailedError` to HTTP once the connection is live.

</Steps>

## On Starlight

<CardGrid>
  <Card title="PostgreSQL setup" icon="document">
    <p><a href="/knowledge-base/nestjs/data/typeorm/postgresql-setup/"><code>forRootAsync</code></a>: <code>ConfigService</code>, <code>forFeature</code>, standalone <code>DataSource</code> for CLI migrations.</p>
  </Card>
  <Card title="Database errors" icon="error">
    <p><a href="/knowledge-base/nestjs/data/typeorm/handle-database-errors/">Exception filter</a>: SQLSTATE <code>23505</code>, named constraints, service-level domain codes.</p>
  </Card>
</CardGrid>

## Pending (vault only)

- Transactions: `dataSource.transaction()` vs `@Transactional` decorators
- Relations and eager/lazy loading
- Query builder patterns
- Custom repositories
