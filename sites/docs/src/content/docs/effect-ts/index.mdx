---
title: Effect-TS
description: Typed errors, dependency injection, and structured concurrency in TypeScript.
related:
  - "[[effect-ts/platform]]"
  - "[[effect-ts/streams]]"
  - "[[effect-ts/concurrency]]"
  - "[[effect-ts/observability]]"
  - "[[effect-ts/fault-tolerant-ingestion]]"
  - "[[effect-ts/state]]"
  - "[[effect-ts/async-result]]"
source:
  - https://github.com/Effect-TS/effect/blob/main/packages/effect/src/Stream.ts
---

import { Aside } from "@astrojs/starlight/components";
import LearningRoadmap from '/src/components/interactive/LearningRoadmap.astro';

> Effect is a TypeScript library for building robust applications with [[effect-ts/typed-errors|typed errors]], dependency injection, and [[effect-ts/concurrency|structured concurrency]] (child fibers cancelled with their parent scope). Programs are values (`Effect<A, E, R>`) that a runtime executes; everything you do with them stays type-safe.

## TL;DR

- The core type is [`Effect<Success, Error, Requirements>`](https://effect.website/docs/getting-started/the-effect-type/#type-parameters), abbreviated `A`, `E`, `R`. Errors and dependencies are tracked in the type, not lost in `try/catch` and DI containers.
- Effect values are **lazy descriptions**, not running code. A runtime ([`Effect.runPromise` / `Effect.runSync` / `Effect.runFork`](https://effect.website/docs/getting-started/running-effects/)) executes them when you ask.
- Replaces (and absorbs) [`fp-ts`](https://github.com/gcanti/fp-ts): per its README, fp-ts is "officially merging with the Effect-TS ecosystem" and "Effect-TS can be regarded as the successor to fp-ts v2".
- Single npm package for the core (`effect`); the rest of the ecosystem ships under `@effect/*` ([[effect-ts/platform|platform]], cli, sql, ai, workflow, rpc, cluster, [[effect-ts/observability|opentelemetry]], vitest, …).
- Latest stable: check [npm `effect`](https://www.npmjs.com/package/effect) before citing a version in prose.

## When to use

- **Use Effect** for: backend services with non-trivial error taxonomies, data pipelines (typed [[effect-ts/streams|streams]] with backpressure: `Stream` is pull-based, so a slow downstream operator naturally throttles upstream production without manual buffer management; [Stream.ts#L57-L61](https://github.com/Effect-TS/effect/blob/main/packages/effect/src/Stream.ts#L57-L61)), CLI tools (`@effect/cli`), durable workflows (`@effect/workflow`), [[effect-ts/schema|schema]]-first apps (built-in `Schema` module), large-language-model (LLM) agents (`@effect/ai`).
- **Don't use Effect** for: tiny scripts where the runtime overhead and learning curve outweigh the wins; teams unwilling to learn generator syntax (`Effect.gen(function* () { ... })`) and [[effect-ts/composition|composition]] idioms.
- **Adoption signal**: compare weekly npm downloads for `effect` vs your incumbent stack when making a team decision; library momentum and job-market demand are different questions.

## Mental model

```ts twoslash title="effect-mental-model.ts"
import { Effect } from "effect";

// Effect<void, never, never> — success void, no errors, no services required
const program = Effect.sync(() => {
  console.log("lazy until run");
});
```

| Concept           | Shape                                                                                              | Reader hook                                                               |
| ----------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `Effect<A, E, R>` | Value describing a computation that, when run, either yields `A`, fails with `E`, or asks for `R`. | A typed `Promise` that also tracks errors and dependencies (see [What is Effect](/knowledge-base/effect-ts/what-is-effect/)). |
| Runtime           | The thing that _executes_ an `Effect`.                                                             | Like calling a thunk: nothing happens until `Effect.runPromise(program)` (see [What is Effect](/knowledge-base/effect-ts/what-is-effect/)). |
| Layers (`R`)      | Typed dependency graph.                                                                            | DI container, but the compiler tells you when a dependency is missing (see [Layers and DI](/knowledge-base/effect-ts/layers-and-di/)). |
| `Effect.gen`      | Generator-based DSL: `yield*` an effect to "await" it.                                             | `async/await` for `Effect` (see [Composition](/knowledge-base/effect-ts/composition/)). |

See [[effect-ts/what-is-effect|What is Effect]] for the longer version.

## Learning Roadmap



<LearningRoadmap
  trackId="effect-ts"
  title="Effect-TS Learning Path"
  nodes={[
    { id: "what-is-effect", label: "What is Effect", href: "/knowledge-base/effect-ts/what-is-effect/", description: "Mental model, laziness, and the three channels", category: "required", group: "Core" },
    { id: "quickstart", label: "Quickstart", href: "/knowledge-base/effect-ts/quickstart/", description: "Install, write, and run your first effect", category: "required", group: "Core", prereqs: ["what-is-effect"] },
    { id: "composition", label: "Composition", href: "/knowledge-base/effect-ts/composition/", description: "pipe, gen, and fn — when to reach for each", category: "required", group: "Core", prereqs: ["quickstart"] },
    { id: "typed-errors", label: "Typed Errors", href: "/knowledge-base/effect-ts/typed-errors/", description: "The E channel: try, catchTag, tagged errors", category: "required", group: "Core", prereqs: ["composition"] },
    { id: "schema", label: "Schema", href: "/knowledge-base/effect-ts/schema/", description: "Runtime boundaries: decode unknown input into typed effects", category: "required", group: "Intermediate", prereqs: ["typed-errors"] },
    { id: "layers-and-di", label: "Layers & DI", href: "/knowledge-base/effect-ts/layers-and-di/", description: "The R channel: Context.Tag, Layer, compile-time DI", category: "required", group: "Intermediate", prereqs: ["typed-errors"] },
    { id: "retry-and-schedule", label: "Retry & Schedule", href: "/knowledge-base/effect-ts/retry-and-schedule/", description: "Bounded retries and backoff policies", category: "optional", group: "Intermediate", prereqs: ["composition"] },
    { id: "scoped-resources", label: "Scoped Resources", href: "/knowledge-base/effect-ts/scoped-resources/", description: "acquireRelease, Scope, and safe cleanup", category: "required", group: "Intermediate", prereqs: ["layers-and-di"] },
    { id: "concurrency", label: "Concurrency & Fibers", href: "/knowledge-base/effect-ts/concurrency/", description: "Fibers, fork, and bounded execution", category: "required", group: "Advanced", prereqs: ["composition"] },
    { id: "streams", label: "Streams", href: "/knowledge-base/effect-ts/streams/", description: "Pull-based streams and backpressure", category: "required", group: "Advanced", prereqs: ["concurrency"] },
    { id: "state", label: "State Coordinates", href: "/knowledge-base/effect-ts/state/", description: "Ref, SubscriptionRef, and STM", category: "optional", group: "Advanced", prereqs: ["typed-errors"] },
    { id: "async-result", label: "AsyncResult", href: "/knowledge-base/effect-ts/async-result/", description: "Reactive state machines for remote async lifecycles", category: "optional", group: "Advanced", prereqs: ["state"] },
    { id: "observability", label: "Observability", href: "/knowledge-base/effect-ts/observability/", description: "Structured telemetry, logging, OpenTelemetry", category: "optional", group: "Advanced", prereqs: ["layers-and-di"] },
    { id: "platform", label: "Platform Layers", href: "/knowledge-base/effect-ts/platform/", description: "Runtime-agnostic FileSystem, HttpClient, routing", category: "optional", group: "Advanced", prereqs: ["layers-and-di"] },
    { id: "fault-tolerant-ingestion", label: "Ingestion Capstone", href: "/knowledge-base/effect-ts/fault-tolerant-ingestion/", description: "Fault-tolerant pipeline recipe combining everything", category: "required", group: "Capstone", prereqs: ["streams", "scoped-resources", "schema"] },
    { id: "ecosystem-map", label: "Ecosystem Map", href: "/knowledge-base/effect-ts/ecosystem-map/", description: "Lookup reference for @effect/* packages", category: "reference", group: "Reference" },
    { id: "layers-vs-nestjs-di", label: "Layers vs NestJS DI", href: "/knowledge-base/effect-ts/layers-vs-nestjs-di/", description: "Compare Effect Layers with NestJS IoC container", category: "comparison", group: "Reference", prereqs: ["layers-and-di"] },
  ]}
/>

After the roadmap above, the [[effect-ts/ecosystem-map|Ecosystem map]] is a lookup reference for `@effect/*` packages.

## Pending notes (vault)

- **Durability Boundaries**: Establishing reliable error recovery thresholds by contrasting in-memory `Effect.retry` scheduling patterns against durable distributed queues and workflow engines.

## See also

- [[effect-ts/quickstart|Quickstart]]
- [[effect-ts/concurrency|Concurrency]]
- [[effect-ts/streams|Streams]]
- [[effect-ts/schema|Schema]]
- [[effect-ts/layers-vs-nestjs-di|Layers vs NestJS DI]]
- [Effect documentation](https://effect.website/docs/getting-started/introduction/) (official)
- [Effect-TS/effect on GitHub](https://github.com/Effect-TS/effect)
