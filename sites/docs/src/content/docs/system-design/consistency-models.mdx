---
title: CAP Theorem & Consistency Models
description: Exploring the trade-offs of the CAP theorem, linearizability vs sequential consistency, and client-centric consistency guarantees in distributed systems.
related:
  - "[[system-design/back-of-the-envelope-estimation]]"
  - "[[system-design/distributed-caching]]"
  - "[[system-design/logical-clocks]]"
  - "[[system-design/consistent-hashing]]"
  - "[[system-design/message-queues]]"
  - "[[system-design/rate-limiting]]"
  - "[[system-design/distributed-transactions]]"
  - "[[system-design/messaging-patterns]]"
  - "[[system-design/delivery-semantics]]"
  - "[[system-design/dead-letter-queues]]"
  - "[[system-design/database-sharding-and-scaling]]"
prerequisites: []
---

> Navigating the trade-offs of the CAP theorem, linearizability, sequential consistency, causal consistency, and client-centric guarantees.

import { Aside, Steps, Tabs, TabItem } from "@astrojs/starlight/components";
import PipelineStrip from '/src/components/interactive/PipelineStrip.astro';
import DecisionGuide from '/src/components/interactive/DecisionGuide.astro';
import FlowSimulator from '/src/components/interactive/FlowSimulator.astro';
import CapTheoremSimulator from '/src/components/interactive/CapTheoremSimulator.astro';
import SequenceDiagram from '/src/components/interactive/SequenceDiagram.astro';

<Aside type="note" title="Prerequisites">

This note is part of the **[[system-design|Systems Design Track]]**. Before resolving CAP theorem partition trade-offs and consistency spectrums, make sure you understand:
- [[system-design/rate-limiting|Rate Limiting & Traffic Shaping]]: Master token bucket, leaky bucket, sliding window counter algorithms, and gateway traffic shaping defenses.
- [[system-design/logical-clocks|Logical Clocks & Time Ordering]]: Master happen-before relations, Lamport timestamps, and Vector Clocks for tracking event ordering and conflicts.

</Aside>

In a single-node system, data consistency is trivial: every write is instantly visible to subsequent reads. In a distributed system where data is replicated across multiple geographical locations, we must coordinate how and when updates propagate. 

A consistency model defines the contract between a distributed database and its client applications. It guarantees what values a client can expect to read when multiple processes execute concurrent reads and writes across replicated nodes.

---

## The Core Trade-off: CAP Theorem Explained

The CAP theorem states that a distributed data store can simultaneously provide at most two of the following three guarantees:

*   **Consistency (Linearizability)**: Every read receives the most recent write or an error.
*   **Availability**: Every non-failing node returns a non-error response for every request, without guaranteeing that it contains the most recent write.
*   **Partition Tolerance**: The system continues to operate despite an arbitrary number of messages being dropped or delayed by the network between nodes.

### The "Branch Office" Partition Analogy
To build immediate intuition, imagine a bank with two physical branches (one in London and one in New York). Both branches have local databases containing customer account balances. Under normal conditions, when a customer deposits money in London, the system instantly notifies the New York branch over the internet, and both databases match.

Now, imagine a **Network Partition (P)**: the fiber-optic cable under the Atlantic Ocean is cut. London and New York can no longer talk to each other. 

At this exact moment, a customer walks into the New York branch and wants to withdraw $100. We are forced to choose:
1.  **Prioritize Consistency (CP)**: New York says, "We have lost contact with London, so we don't know if you spent your money there. We must refuse your withdrawal request." The system remains consistent (no overdrafts can occur), but we have lost **Availability** (we refused a valid request).
2.  **Prioritize Availability (AP)**: New York says, "We cannot talk to London, but we want to be helpful. Here is your $100." The system remains **Available**, but we have sacrificed **Consistency**: if the customer had already withdrawn their entire balance in London five minutes ago, the databases are now out of sync, and the account is overdrawn.

Because physical network cuts (partitions) are an inevitable fact of life, **Partition Tolerance (P)** is mandatory. We can never choose "CA" (Consistency + Availability without Partition Tolerance) because we cannot prevent network failures. When a partition occurs, we must choose either **CP** or **AP**.

<CapTheoremSimulator />

<DecisionGuide
  title="CAP trade-off navigator"
  steps={[
    { id: "start", question: "Is a network partition currently happening?", note: "Partitions are unavoidable in real networks : the question is what to sacrifice when one occurs.", options: [
      { label: "No : the network is healthy", next: "r-both" },
      { label: "Yes : nodes cannot communicate", next: "q-sacrifice" },
    ]},
    { id: "q-sacrifice", question: "Which guarantee do you sacrifice during the partition?", note: "You cannot have both Consistency and Availability while the partition lasts.", options: [
      { label: "Sacrifice Availability (refuse requests until partition heals)", next: "r-cp" },
      { label: "Sacrifice Consistency (serve possibly stale data)", next: "r-ap" },
    ]},
    { id: "r-both", result: "No trade-off needed", explanation: "When the network is healthy, the system can provide both Consistency and Availability. The PACELC extension reminds you that even then, you still trade Latency vs Consistency." },
    { id: "r-cp", result: "CP system (Consistent + Partition-tolerant)", explanation: "The system refuses to serve reads/writes it cannot guarantee are up-to-date. Examples: Google Spanner, CockroachDB, ZooKeeper. Clients may see errors or timeouts during a partition, but data integrity is never compromised.", link: "#1-linearizability-strong-consistency" },
    { id: "r-ap", result: "AP system (Available + Partition-tolerant)", explanation: "The system continues serving requests using local state, accepting that replicas may diverge. Examples: DynamoDB, Cassandra (default), CouchDB. Conflicts are reconciled after the partition heals using strategies like LWW or CRDTs.", link: "#4-eventual-consistency" },
  ]}
/>

---

## The Consistency Spectrum

Distributed databases do not offer a binary choice between strong consistency and eventual consistency. Instead, systems occupy different points on a broad spectrum of guarantees.

<PipelineStrip
  title="Consistency spectrum: strongest → weakest"
  stages={[
    { label: "Linearizability", color: "#34d399", tip: "Single global order of all operations matching real-time. Highest coordination cost : requires distributed consensus (Paxos/Raft).", href: "#1-linearizability-strong-consistency" },
    { label: "Sequential Consistency", color: "#60a5fa", tip: "All nodes see the same global sequence, and each process's operations appear in program order. Real-time ordering is relaxed.", href: "#2-sequential-consistency" },
    { label: "Causal Consistency", color: "#f59e0b", tip: "Causally related operations are ordered consistently everywhere. Concurrent (independent) operations may appear in different orders on different nodes.", href: "#3-causal-consistency" },
    { label: "Eventual Consistency", color: "#f87171", tip: "Replicas converge to the same value given no new writes. Zero real-time coordination : fastest and cheapest, but clients may read stale data.", href: "#4-eventual-consistency" },
  ]}
/>

| Consistency Model | Description | Coordination Cost | Typical Systems |
| :--- | :--- | :--- | :--- |
| [**Linearizable**](#1-linearizability-strong-consistency) | Strict real-time global ordering. Reads always return the latest write. | Extremely High (Consensus required) | Google Spanner, CockroachDB |
| [**Sequential**](#2-sequential-consistency) | Identical global order of operations seen by all nodes. Real-time is ignored. | High (Global logical sequencing) | Apache ZooKeeper (for writes) |
| [**Causal**](#3-causal-consistency) | Causally related operations are seen in the same order. Concurrent updates can diverge. | Medium (Vector tracking) | Cassandra (with lightweight transactions) |
| [**Eventual**](#4-eventual-consistency) | Replicas converge to the same value over time if no new writes occur. | Low (Background anti-entropy) | DynamoDB, CouchDB, DNS |

*Note: For detail on partitioning strategies and hashing techniques used to distribute these data sets, see [[system-design/consistent-hashing|Consistent Hashing]].*

---

## Data-Centric Consistency Models

Data-centric consistency models define system-wide, global constraints on the order in which all operations are executed.

### 1. Linearizability (Strong Consistency)

Linearizability is the strongest possible consistency guarantee. It imposes a strict real-time constraint: as soon as a write completes successfully, every subsequent read across any server in the system must return that new value or a newer one.

**The Google Doc Analogy**:
Imagine a shared Google Doc in "real-time" editing mode. If Alice edits a sentence on her screen and receives confirmation that the edit is saved, Bob (reading the same document a split second later) is guaranteed to see Alice's new sentence. He will never see the old, stale version. 
In distributed databases, enforcing this "instant shared vision" is extremely expensive. It requires all servers to coordinate and agree before a write is confirmed to the user, ensuring no server falls behind.

<FlowSimulator
  title="Linearizability: Violation vs. Compliance"
  nodes={[
    { id: "writer", label: "Client 1 (Writer)", col: 0 },
    { id: "reader", label: "Client 2 (Reader)", col: 0 },
    { id: "nodeA", label: "Replica Node A", col: 1 },
    { id: "nodeB", label: "Replica Node B", col: 2 },
  ]}
  flows={[
    { label: "Linearizability Violation", steps: [
      { from: "writer", to: "nodeA", message: "Write(x = 5)", description: "Client 1 writes a new value x = 5 to Replica Node A." },
      { from: "nodeA", to: "writer", message: "Write Success", description: "Replica Node A processes the write locally and acknowledges success back to Client 1." },
      { from: "reader", to: "nodeA", message: "Read(x)", description: "Client 2 reads from Replica Node A and receives the new value x = 5." },
      { from: "nodeA", to: "reader", message: "Returns x = 5", description: "Replica Node A responds with x = 5 (New Value)." },
      { from: "reader", to: "nodeB", message: "Read(x)", description: "Client 2 immediately queries Replica Node B for x." },
      { from: "nodeB", to: "reader", message: "❌ Returns x = 0 (Stale!)", description: "Because asynchronous replication is lagging, Replica Node B still holds the old value x = 0. Client 2 reads a stale value after already seeing the newer value : violating Linearizability!" },
    ]},
    { label: "Linearizable Execution", steps: [
      { from: "writer", to: "nodeA", message: "Write(x = 5)", description: "Client 1 writes a new value x = 5 to Replica Node A." },
      { from: "nodeA", to: "writer", message: "Write Success", description: "Replica Node A processes the write and acknowledges success." },
      { from: "reader", to: "nodeA", message: "Read(x)", description: "Client 2 reads from Replica Node A." },
      { from: "nodeA", to: "reader", message: "Returns x = 5", description: "Replica Node A returns the latest value x = 5." },
      { from: "reader", to: "nodeB", message: "Read(x)", description: "Client 2 queries the lagging Replica Node B." },
      { from: "nodeB", to: "nodeA", message: "Sync Check", description: "Before responding, Replica Node B triggers a synchronous quorum read or sync check against Replica Node A." },
      { from: "nodeA", to: "nodeB", message: "x = 5 (Latest)", description: "Replica Node A shares the latest committed value x = 5." },
      { from: "nodeB", to: "reader", message: "✅ Returns x = 5", description: "Replica Node B updates its state and returns the fresh value x = 5. Client 2 receives a compliant, real-time ordered view." },
    ]}
  ]}
  speed={1400}
/>

### 2. Sequential Consistency

Sequential consistency is slightly weaker than linearizability. It guarantees that all operations appear to be executed in a single, global sequence, and that the operations of each individual user appear in this sequence in the order they were sent.

**The Multi-Player Chat Analogy**:
Unlike linearizability, there is no real-time clock constraint. 
Imagine a global group chat. If Alice posts "Hello" at 10:00 AM and Bob posts "World" at 10:01 AM, sequential consistency allows the system to display Bob's message before Alice's message, as long as every single user in the chat room sees the messages in that exact same order (Bob then Alice). The timeline is allowed to drift from real-world wall-clock time, but everyone must share the exact same sequence of history.

### 3. Causal Consistency

Causal consistency guarantees that operations that are causally related (meaning one event influenced or caused another) must be seen in the same order by every node in the system. Operations that are concurrent (independent and unrelated) can be seen in different orders by different nodes.

**The Threaded Slack Analogy**:
Imagine a threaded conversation on Slack.
*   **Causal Relationship**: Alice asks "Do you want pizza?" and Bob replies "Yes!". Bob's reply is caused by Alice's question. Under causal consistency, every user in the workspace is guaranteed to see Alice's question before Bob's reply. It is impossible to see "Yes!" floating in isolation before the question is visible.
*   **Concurrent (Unrelated) Events**: In a completely different channel, Dave posts "It is raining in Seattle." This post has no relationship to the pizza conversation. Causal consistency allows different users to see Dave's post and Alice's question in different orders, because they are independent events.

<SequenceDiagram
  title="Causal consistency sequence: ordered post replication"
  actors={[
    { id: "alice", label: "Alice", color: "#60a5fa" },
    { id: "bob", label: "Bob", color: "#818cf8" },
    { id: "replicaA", label: "Replica A", color: "#a78bfa" },
    { id: "replicaB", label: "Replica B", color: "#f472b6" },
    { id: "charlie", label: "Charlie", color: "#fbbf24" }
  ]}
  steps={[
    { from: "alice", to: "replicaA", message: "Post A ('Hello!')", type: "solid", description: "Alice writes Post A ('Hello!') to Replica A." },
    { from: "replicaA", to: "replicaB", message: "Replicate Post A", type: "solid", description: "Replica A replicates Post A to Replica B asynchronously." },
    { from: "bob", to: "replicaB", message: "Read Posts", type: "solid", description: "Bob reads from Replica B and observes Post A." },
    { from: "bob", to: "replicaB", message: "Post B ('Hi Alice!')", type: "solid", description: "Bob posts a reply, which is causally dependent on Post A." },
    { from: "charlie", to: "replicaA", message: "Read Posts", type: "solid", description: "Charlie reads from Replica A." },
    { from: "replicaA", to: "charlie", message: "Returns [Post A]", type: "dashed", description: "Replica A returns Post A. Replica B has not replicated Post B to Replica A yet, so Post B is not visible." },
    { from: "replicaB", to: "replicaA", message: "Replicate Post B", type: "solid", description: "Replica B replicates Post B to Replica A asynchronously." },
    { from: "charlie", to: "replicaA", message: "Read Posts", type: "solid", description: "Charlie reads from Replica A again." },
    { from: "replicaA", to: "charlie", message: "Returns [Post A, Post B]", type: "dashed", description: "Replica A now has both posts, and Charlie receives them in the correct causal order." }
  ]}
/>

If Dave and Eva write concurrent, independent updates, their orders do not need to match across different nodes. They are merged deterministically using strategies like Last-Write-Wins (LWW) or Conflict-Free Replicated Data Types (CRDTs).

<SequenceDiagram
  title="Concurrent updates resolved via Last-Write-Wins"
  actors={[
    { id: "dave", label: "Dave", color: "#60a5fa" },
    { id: "eva", label: "Eva", color: "#34d399" },
    { id: "replicaA", label: "Replica A", color: "#a78bfa" },
    { id: "replicaB", label: "Replica B", color: "#f472b6" },
    { id: "charlie", label: "Charlie", color: "#fbbf24" },
    { id: "frank", label: "Frank", color: "#f87171" }
  ]}
  steps={[
    { from: "dave", to: "replicaA", message: "Update(Avatar = Blue)", type: "solid", description: "Dave updates his avatar to Blue on Replica A at timestamp t=100." },
    { from: "eva", to: "replicaB", message: "Update(Avatar = Red)", type: "solid", description: "Eva updates her avatar to Red on Replica B at timestamp t=101." },
    { from: "replicaA", to: "replicaB", message: "Replicate Blue (t=100)", type: "solid", description: "Replica A replicates Dave's write to Replica B." },
    { from: "replicaB", to: "replicaA", message: "Replicate Red (t=101)", type: "solid", description: "Replica B replicates Eva's write to Replica A." },
    { from: "charlie", to: "replicaA", message: "Read Avatar", type: "solid", description: "Charlie reads from Replica A." },
    { from: "replicaA", to: "charlie", message: "Returns Red", type: "dashed", description: "Replica A returns Red because Red (t=101) won over Blue (t=100) via Last-Write-Wins." },
    { from: "frank", to: "replicaB", message: "Read Avatar", type: "solid", description: "Frank reads from Replica B." },
    { from: "replicaB", to: "frank", message: "Returns Red", type: "dashed", description: "Replica B returns Red as both replicas converged deterministically." }
  ]}
/>

### 4. Eventual Consistency

Eventual consistency is the weakest model, but it is highly popular because it requires zero real-time coordination, making it extremely fast. It guarantees that if no new updates are made, all replicas will eventually sync up and hold the identical value.

**The Physical Newspaper Analogy**:
Imagine a news agency printing a newspaper in different cities (London, New York, and Tokyo). 
In the morning, the print editions might show slightly different headlines because of different local print deadlines and printing speeds (lagging updates). However, by the end of the day, as journalists share all reports and update the online digital archives, all editions eventually converge to show the exact same complete history. In the meantime, readers in London and New York might temporarily see different headlines, but the system does not crash or block updates.

---

## Client-Centric Consistency Models

When clients move between different replica nodes (for example, a mobile client reconnecting to different cell towers or CDN edge caches), they need guarantees about their own personal session histories.

### 1. Read-Your-Writes

Guarantees that a client will always see their own successful updates. 

**The Social Media Profile Update Analogy**:
Imagine you update your social media bio from "Student" to "Software Engineer". When you hit save, the request goes to the primary database server. If you refresh your profile immediately, and your read request is routed to a lagging database replica that hasn't received the update yet, your bio would temporarily show "Student" again. This is a violation. Read-Your-Writes ensures that your personal sessions are pinned or stickily routed so you always see your own edits immediately.

<Tabs>
  <TabItem label="Read-Your-Writes Violation">
    <SequenceDiagram
      title="Read-Your-Writes Violation"
      actors={[
        { id: "client", label: "Client App", color: "#60a5fa" },
        { id: "primary", label: "Primary Node", color: "#a78bfa" },
        { id: "replica", label: "Read Replica", color: "#f472b6" }
      ]}
      steps={[
        { from: "client", to: "primary", message: "UpdateProfile(Bio = 'Developer')", type: "solid", description: "Client writes profile update to the primary node." },
        { from: "primary", to: "client", message: "Success", type: "dashed", description: "Primary node successfully registers the update." },
        { from: "client", to: "replica", message: "GetProfile()", type: "solid", description: "Client immediately refreshes the page and reads from a lagging read replica." },
        { from: "replica", to: "client", message: "Returns Bio = ''", type: "dashed", color: "#f87171", description: "Because replication is lagging, the replica returns the old value, creating a violation where the user thinks their update was lost." }
      ]}
    />
  </TabItem>
  <TabItem label="Sticky Router Workaround">
    <SequenceDiagram
      title="Sticky router workaround for Read-Your-Writes"
      actors={[
        { id: "client", label: "Client App", color: "#60a5fa" },
        { id: "router", label: "Session Router", color: "#fbbf24" },
        { id: "primary", label: "Primary Node", color: "#a78bfa" },
        { id: "replica", label: "Read Replica", color: "#f472b6" }
      ]}
      steps={[
        { from: "client", to: "router", message: "UpdateProfile(Bio = 'Developer')", type: "solid", description: "Client sends profile update through the session router." },
        { from: "router", to: "primary", message: "UpdateProfile", type: "solid", description: "Router forwards the write to the primary node." },
        { from: "primary", to: "router", message: "Success", type: "dashed", description: "Primary node confirms successful write." },
        { from: "router", to: "client", message: "Success (Set Cookie)", type: "dashed", description: "Router returns success and sets a sticky session cookie on the client." },
        { from: "client", to: "router", message: "GetProfile()", type: "solid", description: "Client refreshes immediately, passing the sticky session cookie." },
        { from: "router", to: "primary", message: "GetProfile()", type: "solid", description: "Router recognizes the sticky session and routes the read to the primary instead of a replica." },
        { from: "primary", to: "router", message: "Returns Bio = 'Developer'", type: "dashed", description: "Primary returns the latest up-to-date data." },
        { from: "router", to: "client", message: "Returns Bio = 'Developer'", type: "dashed", description: "Client receives their own write successfully." }
      ]}
    />
  </TabItem>
</Tabs>

### 2. Monotonic Reads

Guarantees that if a client reads a certain piece of data, any subsequent read they perform will never return an older version. The system will never appear to "go back in time."

**The Feed Scroll Subscriber Analogy**:
Imagine you are scrolling through a video channel and see a subscriber count of 10,000. You refresh the page a second later, and your request lands on a lagging server that reports 9,950 subscribers. The count appears to have shrunk, which feels like time travel. Monotonic reads prevents this by ensuring that once your session has observed a value of 10,000, all subsequent reads in your session are guaranteed to return 10,000 or more, never less.

### 3. Monotonic Writes

Guarantees that a single client's write operations are processed sequentially in the exact order they were submitted.

**The Comment Edit and Delete Analogy**:
Imagine you post a comment on an article, then immediately edit it to fix a typo, and then decide to delete it entirely. Monotonic writes guarantees that these three actions (Post → Edit → Delete) are applied in that exact order on every database replica. Without this, a lagging replica might receive the Delete instruction before the Edit instruction, resulting in the edited comment reappearing on that server forever.

### 4. Writes-Follow-Reads

Guarantees that if a client reads a value and then performs a write, that write will causally follow the read value. Anyone else who sees your write is guaranteed to see the read that triggered it.

**The Comment Reply Analogy**:
Imagine you read a post saying "Who wants to go hiking tomorrow?" (Post A). You immediately post a reply saying "Count me in!" (Post B). Post B only makes sense in the context of Post A. Writes-Follow-Reads guarantees that no user on the site will ever see your reply ("Count me in!") without also being able to see the original invitation post. The reply is causally bound to follow the read that inspired it.

---

## Gotchas and Production Considerations

<Aside type="note" title="The PACELC Extension to CAP">
The CAP theorem only applies when a network partition (P) is active. The **PACELC theorem** expands this: **I**f there is a **P**artition, trade off **A**vailability or **C**onsistency; **E**lse, trade off **L**atency or **C**onsistency. Even in a healthy network, enforcing strong consistency requires cross-replica coordination, which slows down read and write operations.
</Aside>

### 1. Consensus Costs
Achieving linearizability requires distributed consensus algorithms like Paxos or Raft. These protocols incur severe network latency costs:
*   Every write requires round-trips to a quorum of nodes before returning success.
*   A single slow disk or network link on a quorum member can degrade write performance for the entire cluster.

### 2. Reconciling Concurrent Updates
If you choose Availability (AP) and allow concurrent local writes, you must eventually reconcile conflicting updates.
*   **Last-Write-Wins (LWW)**: Uses physical clock timestamps to pick the "latest" write. It is simple but vulnerable to clock skew, which can silently discard updates made on nodes with slightly lagging clocks.
*   **Multi-Value (Siblings)**: The database retains all conflicting concurrent writes as a sibling set, returning `[ValA, ValB]` on client reads. The client application must reconcile this set (either by querying the user or executing automated merge logic).
*   **CRDTs (Conflict-Free Replicated Data Types)**: Uses mathematical structures (like grow-only sets, PN-counters, or observed-removed sets) to merge concurrent updates deterministically without custom resolution code.

<Aside type="note" title="Case Study: Amazon Dynamo & The Shopping Cart Sibling Merge">
Amazon's original Dynamo database pioneered the use of **Multi-Value Sibling Registers** to enforce a strict **"never lose a sale"** business rule. 

During a network partition, if a customer added a "Book" on Server A (London) and "Headphones" on Server B (New York), a standard Last-Write-Wins (LWW) resolution would have silently deleted one of the items based on timestamp (resulting in lost revenue). By storing both values as siblings `[["Book"], ["Headphones"]]`, the shopping cart application server received both states upon the next read and executed a simple `Union()` merge: `Cart = ["Book", "Headphones"]`. The customer sees both items, checkout succeeds, and data loss is avoided!
</Aside>

---

## See also
*   [[system-design/consistent-hashing|Consistent Hashing & Dynamic Ring Partitioning]]
*   [[system-design/logical-clocks|Logical Clocks & Event Ordering]]
