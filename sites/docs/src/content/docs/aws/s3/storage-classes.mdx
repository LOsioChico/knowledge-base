---
title: S3 storage classes
description: S3 storage class comparison — Standard, Intelligent-Tiering, IA, Glacier tiers, and when retrieval fees bite.
related:
  - "[[aws/s3/quickstart]]"
  - "[[aws/s3/lifecycle-rules]]"
prerequisites:
  - "[[aws/s3/quickstart]]"
---

> Every S3 object has a storage class controlling per-GB cost, retrieval cost, latency, and AZ count. Pick wrong and you overpay for idle storage or pay surprise retrieval fees on hot data.

import { Aside } from "@astrojs/starlight/components";
import DecisionGuide from '/src/components/interactive/DecisionGuide.astro';

<Aside type="tip" title="Prerequisites">

This note is part of the **[[aws|AWS Serverless Track]]**. Before analyzing S3 storage classes, make sure you understand:
- [[aws/s3/quickstart|S3 Quickstart]]: Creating buckets and uploading files via AWS CLI.

</Aside>

## TL;DR

- **Default to Standard**: millisecond latency, no retrieval fee, ≥3 AZs.
- **Intelligent-Tiering** when access pattern is unknown: auto-tiering; small per-object monitoring fee; objects &lt;128 KB never tier.
- **Standard-IA / One Zone-IA** only for rarely read data: per-GB **retrieval fee** wipes savings if read more than ~monthly.
- **Glacier classes** for archives: cheapest storage; Flexible/Deep Archive need `RestoreObject` (minutes to hours).
- **Don't use Reduced Redundancy**: AWS recommends against; Standard is cheaper ([source](https://docs.aws.amazon.com/AmazonS3/latest/userguide/storage-class-intro.html#sc-freq-data-access)).

## Reference table

From [AWS storage-class comparison](https://docs.aws.amazon.com/AmazonS3/latest/userguide/storage-class-intro.html#sc-compare):

| Class | API name | Availability | AZs | Min duration | Retrieval fee |
| --- | --- | --- | --- | --- | --- |
| [S3 Standard](/knowledge-base/aws/s3/quickstart/) | `STANDARD` | 99.99% | ≥3 | None | No |
| Intelligent-Tiering | `INTELLIGENT_TIERING` | 99.9% | ≥3 | None | No (monitoring) |
| [Standard-IA](#setting-the-class) | `STANDARD_IA` | 99.9% | ≥3 | 30 days | Yes |
| One Zone-IA | `ONEZONE_IA` | 99.5% | 1 | 30 days | Yes |
| Glacier Instant Retrieval | `GLACIER_IR` | 99.9% | ≥3 | 90 days | Yes |
| [Glacier Flexible Retrieval](#setting-the-class) | `GLACIER` | 99.99% after restore | ≥3 | 90 days | Yes |
| Glacier Deep Archive | `DEEP_ARCHIVE` | 99.99% after restore | ≥3 | 180 days | Yes |

## When to reach for which

**Standard**: data you plan to read; optimize later with [[aws/s3/lifecycle-rules|lifecycle transitions]].

**Intelligent-Tiering**: unknown access; skip for millions of tiny files (&lt;128 KB always billed at Frequent Access rate).

<Aside type="caution" title="IA read more than ~monthly costs more than Standard">

Per-GB retrieval on every GET. Break-even is roughly one full read per month.

</Aside>

**Glacier**: three patience levels: Instant Retrieval (ms), Flexible (async restore tiers), Deep Archive (cheapest, hours).

<Aside type="note" title="Glacier objects use the S3 API">

Despite the name, GET goes through S3: not a separate Glacier console.

</Aside>

**Express One Zone**: single-digit-ms, single AZ; directory buckets; ML/analytics co-located with GPU.

<DecisionGuide
  title="Which S3 storage class do I need?"
  steps={[
    {
      id: "start",
      question: "How often will you access this data?",
      note: "Access frequency is the biggest cost driver.",
      options: [
        { label: "Multiple times per day (hot data)", next: "hot" },
        { label: "Once per month or less", next: "infrequent" },
        { label: "I don't know the pattern yet", next: "r-intelligent" },
        { label: "Almost never : archival / compliance", next: "archive-speed" },
      ],
    },
    {
      id: "hot",
      question: "Do you need the lowest possible latency?",
      options: [
        { label: "Standard millisecond latency is fine", next: "r-standard" },
        { label: "Single-digit-ms, co-located with compute", next: "r-express" },
      ],
    },
    {
      id: "infrequent",
      question: "Do you need multi-AZ durability?",
      note: "One Zone-IA is ~20% cheaper but risks data loss if the AZ goes down.",
      options: [
        { label: "Yes : must survive AZ failure", next: "r-standard-ia" },
        { label: "No : reproducible data (thumbnails, transcodes)", next: "r-onezone-ia" },
      ],
    },
    {
      id: "archive-speed",
      question: "When you do need the data, how fast?",
      options: [
        { label: "Milliseconds : rare but urgent reads", next: "r-glacier-ir" },
        { label: "Minutes to hours is acceptable", next: "r-glacier-flex" },
        { label: "12+ hours is fine : cheapest possible", next: "r-deep-archive" },
      ],
    },
    { id: "r-standard", result: "S3 Standard", explanation: "No retrieval fee, millisecond latency, ≥3 AZs. Best default for any workload you actively read." },
    { id: "r-express", result: "S3 Express One Zone", explanation: "Single-digit-ms latency in a directory bucket co-located with your compute. Use for ML training data or analytics scratch space." },
    { id: "r-intelligent", result: "S3 Intelligent-Tiering", explanation: "AWS auto-tiers objects between Frequent and Infrequent access based on usage. Small per-object monitoring fee. Objects <128 KB always stay at Frequent Access rate." },
    { id: "r-standard-ia", result: "S3 Standard-IA", explanation: "~40% cheaper storage than Standard, but per-GB retrieval fee on every GET. Break-even: roughly one full read per month. ≥3 AZs." },
    { id: "r-onezone-ia", result: "S3 One Zone-IA", explanation: "~20% cheaper than Standard-IA. Single AZ : data lost if that AZ fails. Use for re-creatable data like thumbnails or transcoded media." },
    { id: "r-glacier-ir", result: "Glacier Instant Retrieval", explanation: "Cheapest option with millisecond GET latency. 90-day minimum duration. Retrieval fee applies. Great for quarterly compliance reports." },
    { id: "r-glacier-flex", result: "Glacier Flexible Retrieval", explanation: "Three restore tiers: Expedited (1-5 min), Standard (3-5 hrs), Bulk (5-12 hrs). 90-day minimum. Use for disaster recovery backups." },
    { id: "r-deep-archive", result: "Glacier Deep Archive", explanation: "Cheapest S3 class (~$1/TB/mo). 12-48 hour restore. 180-day minimum. Use for 7-year compliance archives or raw audit logs." },
  ]}
/>

## Setting the class

- At **PUT**: `--storage-class STANDARD_IA` on `aws s3 cp` / SDK.
- Via **lifecycle** `Transition`: most common production pattern.
- At **copy**: `aws s3 cp s3://b/k s3://b/k --storage-class GLACIER`.

## See also

- [[aws/s3|S3]].
- [[aws/s3/lifecycle-rules|Lifecycle rules]].
- [Comparing storage classes](https://docs.aws.amazon.com/AmazonS3/latest/userguide/storage-class-intro.html#sc-compare) (official).
